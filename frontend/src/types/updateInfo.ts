export type UpdateInfo = {
  protocolVersion: string;
  latestVersion: string;
  force: boolean;
  channel: string;
  downloadUrl: string;
  checksum: string;
  releaseNotes: string;
  minSupportedVersion: string;
};
