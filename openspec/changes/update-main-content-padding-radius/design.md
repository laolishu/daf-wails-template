# Design Doc：主内容区圆角与内边距

## 目标
统一主内容区容器的视觉轮廓与内边距，确保内容区在浅色背景下保持轻量卡片感。

## 设计要点
1. **圆角**
   - 统一为 12px，形成明确的轻量卡片视觉。

2. **内边距**
   - 内边距统一为 6px，避免内容贴边，同时不削弱内容区域可用面积。

## 影响范围
- 前端布局：主内容区容器样式调整（圆角与内边距）
- 规范：更新 frontend-layout 与 frontend-guidelines 的相关要求
