# 变更：主内容区圆角与内边距

## 为什么
当前主内容区容器圆角与内边距不统一，导致界面缺乏层次与对齐感。需要明确圆角半径与内边距，以确保主内容区在不同窗口尺寸下保持一致视觉。

## 变更内容
- 主内容区容器圆角统一为 12px
- 主内容区容器内边距统一为 6px

## 影响
- 受影响规范：frontend-layout、frontend-guidelines
- 受影响代码（后续实施阶段）：frontend/src/App.tsx
