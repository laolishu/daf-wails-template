# frontend-layout 规范增量：主内容区圆角与内边距

## 修改需求

### 需求：主内容区圆角
右侧主内容区容器必须应用 12px 圆角。

#### 场景：主内容区渲染
- **当** 主内容区加载完成
- **那么** 右侧主内容区容器必须显示 12px 圆角

---

### 需求：主内容区内边距
右侧主内容区容器必须应用 6px 内边距。

#### 场景：主内容区渲染
- **当** 主内容区加载完成
- **那么** 右侧主内容区容器必须使用 6px 内边距
