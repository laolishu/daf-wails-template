# frontend-shell 规范增量：Sidebar 密度与对齐

## 修改需求

### 需求：Sidebar 导航配置
Sidebar 必须使用深色蓝灰背景，中部为纵向导航菜单（图标 + 文本），当前选中项高亮显示；底部必须放置“设置 / 帮助 / 升级”等次级入口。

#### 场景：导航与次级入口展示
- **当** 用户查看 Sidebar
- **那么** 菜单项必须纵向对齐且横向空白最小化
- **并且** 选中态为浅色圆角块，圆角 8px
- **并且** 选中态内边距左右 8–10px，上下 6px
- **并且** 图标与背景边缘至少保留 8px 间距
