# frontend-ui 规范增量：Sidebar 密度与对齐

## 修改需求

### 需求：布局与交互
前端必须使用 Mantine AppShell，左侧固定 Sidebar，主内容单一焦点。

#### 场景：渲染主布局
- **当** 渲染主界面
- **那么** Sidebar 宽度必须为 200–220px（默认 208px）
- **并且** 菜单项高度必须为 40px
- **并且** 选中态为浅色圆角块（8px），不使用强描边或高对比边框
